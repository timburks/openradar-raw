apiVersion: openapi/v1alpha1
kind: Radar
metadata:
    name: "17265161"
    labels:
        datastore_id: "5862465137016832"
data:
    classification: Enhancement
    created: "2014-06-11T07:08:05.926279Z"
    description: "Swift officially only supports custom operators using a small set of characters.  However, the current Swift compiler accepts many Unicode characters for custom operators.  This allows some elegant constructions:\r\n\r\noperator infix ≤ {associativity none precedence 130}\r\nfunc ≤<T : Comparable>(a: T, b: T) -> Bool {\r\n    return a <= b\r\n}\r\nprintln(1 ≤ 2)\r\n\r\noperator infix → {associativity left precedence 105}\r\nfunc →<ArgType,ResultType>(arg: ArgType, f: (ArgType) -> ResultType) -> ResultType {\r\n    return f(arg)\r\n}\r\n\"arrow\" → println\r\n\r\noperator infix ∑ {associativity none precedence 125}\r\nfunc ∑(sequence: Double[], transform: (Double) -> Double) -> Double {\r\n    return reduce(map(sequence, transform), 0, +)\r\n}\r\nprintln([1, 2, 3, 4] ∑ {$0*$0})\r\n\r\nI request that the current support of Unicode custom operators become an official part of the language."
    email: jlieske@gmail.com
    modified: "2015-03-07T06:10:27.699084Z"
    number: "17265161"
    number_intvalue: 17265161
    originated: 11-Jun-2014 00:06 AM
    parent_number: '&{NULL_VALUE}'
    product: Developer Tools
    product_version: 'Xcode6 beta: Version 6.0 (6A215l)'
    reproducible: Always
    resolved: Resolved
    status: Closed
    title: Swift should officially allow Unicode custom operators

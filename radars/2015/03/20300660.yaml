apiVersion: openapi/v1alpha1
kind: Radar
metadata:
    name: "20300660"
    labels:
        datastore_id: "6350057808855040"
data:
    classification: Enhancement
    created: "2015-03-25T23:03:05.910598Z"
    description: |-
        Summary:
        Currently, there is no way to specify that a UIScrollView should use the content height based on auto layout, but keep the width the same as the width of the scroll views frame (or variable width and fixed height).

        Expected Results:
        Perhaps a dynamic constraint that differentiates between the bounds and the frame of a scroll view. Or simply 2 properties that cause the content width or height to match the frame.

        Or even 2 constraints (available through properties) with low priorities that match the frame to the bounds. With this solution, most constraints would just work, as long as it was greater than the content stretching resistance value.

        Actual Results:
        To achieve this, our options are currently to either specify contentSize manually (and update it any time constraints change) or to create a weird constraint where the left and right of a view are set to both the scroll views left and right and it’s superviews left and right.
    email: davbeck@gmail.com
    modified: "2015-03-25T23:03:05.910805Z"
    number: "20300660"
    number_intvalue: 20300660
    originated: 25-Mar-2015 04:02 PM
    parent_number: '&{NULL_VALUE}'
    product: iOS SDK
    product_version: iOS 8.2
    reproducible: Always
    resolved: ""
    status: Open
    title: Add better way to specify UIScrollView contentSize
